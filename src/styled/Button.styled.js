import styled from 'styled-components';

export const AppButton = styled.button`
  background: #bf4f74;
  color: white;
  font-size: 1em;
  margin: 1em;
  padding: 0.25em 1em;
  border: 2px solid #bf4f74;
  border-radius: 3px;
  cursor: pointer;

  &:hover {
    background: #a33e5c;
  }
`;
